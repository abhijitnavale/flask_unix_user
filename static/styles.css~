body {
    margin: 0 auto;
    width: 960px;
}
td {
    padding-top: 10px;
    padding-bottom: 20px;
}
td.user-actions {
    padding-left: 50px;
}
tr {
    border-bottom: 1px solid green;
}
dialog {
    visibility: hidden;
    transform: scale(0.1);
    transition: transform 200ms;
}
dialog.dialog-scale {
    visibility: visible;
    transform: scale(1);
}
